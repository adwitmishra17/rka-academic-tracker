{
  "name": "rka-resync-exam-terms",
  "version": "1.0.0",
  "description": "Touches every Firestore examTerms doc so the (new) syncExamTerms Cloud Function fires and backfills Supabase exam_terms. Terms must reach Supabase as real UUIDs before the teacher PWA term picker and the term→paper→marks chain can work.",
  "main": "resync.js",
  "scripts": {
    "dry": "node resync.js --dry-run",
    "resync": "node resync.js --confirm"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
